import { ArrowRight, FileText, FolderOpen, History } from "lucide-react";
import { Link } from "react-router-dom";
import { categories, documents } from "../data/documents";
export function AdminDashboardPage() { const recent = [...documents].sort((a,b) => b.updatedAt.localeCompare(a.updatedAt))[0]; return <main className="admin-content"><div className="admin-page-heading"><p className="eyebrow">Overview</p><h1>Document Management</h1><p>Manage the documents available on the public website.</p></div><div className="admin-stats"><article><FileText /><span>Total documents</span><strong>{documents.length}</strong></article><article><FolderOpen /><span>Total categories</span><strong>{categories.length}</strong></article><article><History /><span>Recently updated</span><strong>{recent?.title}</strong></article></div><section className="admin-callout"><div><h2>Keep community records current</h2><p>Review the public library and prepare documents for the next update.</p></div><Link className="primary-button" to="/admin/documents">Manage Documents <ArrowRight size={18} /></Link></section></main>; }
